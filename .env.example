# Target the dev proxy at your data backend (see vite.config.ts).
# Currently the app calls the Taskade API via the /api/taskade prefix; point
# this at whatever backend you switch to.
TASKADE_GATEWAY_URL=https://www.taskade.com
